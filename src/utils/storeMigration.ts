// Fresh SAT Prep app — version 1, no migrations needed yet.
// This file exists as a placeholder for future migrations.

export function migrateState(persistedState: unknown): unknown {
  return persistedState;
}
